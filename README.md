# UniReg Demo

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Start the app:

```bash
npm run dev
```

3. Open `http://localhost:3000`

## Notes

- The repo already includes the demo `.env` setup and SQLite database.
- If you want a production build locally, run `npm run build` then `npm run start`.
